const Title = ({children}) => {
    return (
        <span className='xl:text-6xl text-5xl bg-gradient-to-bl bg-clip-text text-red-500 from-darkRed to-lightRed font-bold'>{`  ${children}`}</span>
    )}
export default Title;